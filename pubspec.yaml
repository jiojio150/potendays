name: potendays
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.2.1
  kakao_flutter_sdk: ^1.9.1
  flutter_dotenv: ^6.0.1
  # 여기에 있던 패키지들을 모두 dependencies 쪽으로 옮겼어요!
  cupertino_icons: ^1.0.8
  firebase_core: ^4.7.0
  cloud_firestore: ^6.3.0
  firebase_auth: ^6.4.0
  intl: ^0.19.0
  google_maps_flutter: ^2.17.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env